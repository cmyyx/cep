/**
 * Legal section shared layout.
 * Individual pages (privacy, terms) provide their own generateMetadata.
 */
export default function LegalLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
